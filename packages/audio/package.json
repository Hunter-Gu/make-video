{
  "name": "@make-video/audio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p ../../tsconfig.packages.json && esbuild src/cli.ts --bundle --minify --platform=node --format=esm --target=node22 --outfile=../../skills/make-video/scripts/audio.mjs --banner:js='import {createRequire} from \"node:module\"; const require = createRequire(import.meta.url);'"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "dependencies": {
    "@make-video/ai": "workspace:*",
    "@make-video/project": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
