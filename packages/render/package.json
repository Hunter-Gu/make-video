{
  "name": "@make-video/render",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/cli.ts --bundle --minify --platform=node --format=esm --target=node22 --outfile=../../skills/make-video/scripts/render.mjs"
  },
  "dependencies": {
    "@make-video/assets": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
