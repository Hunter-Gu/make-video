{
  "name": "@make-video/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p ../../tsconfig.packages.json && esbuild src/cli.ts --bundle --minify --platform=node --format=esm --target=node22 --outfile=../../skills/make-video/scripts/ai.mjs --banner:js='import {createRequire} from \"node:module\"; const require = createRequire(import.meta.url);'"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "dependencies": {
    "@ai-sdk/google": "^4.0.49",
    "@make-video/project": "workspace:*",
    "ai": "^7.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
