import{createHash as x}from"node:crypto";import{existsSync as m,linkSync as g,lstatSync as A,mkdirSync as S,readFileSync as C,statSync as O,unlinkSync as P}from"node:fs";import{dirname as L,relative as R,resolve as D,sep as J}from"node:path";import{existsSync as E,readFileSync as $}from"node:fs";import{isAbsolute as k,relative as f,resolve as u,sep as l}from"node:path";var p=process.env.MAKE_VIDEO_PROJECT_ROOT??process.cwd(),v=/^[a-z0-9]+(?:-[a-z0-9]+)*$/,j=(t,r)=>{if(typeof t!="string"||t.length===0)throw new Error(`${r} must be a non-empty project-relative path.`);if(k(t))throw new Error(`${r} must not be absolute: ${t}`);let e=u(p,t),o=f(p,e);if(o===".."||o.startsWith(`..${l}`))throw new Error(`${r} escapes the project: ${t}`);return e},h=t=>{if(!v.test(t))throw new Error(`Invalid video id "${t}".`);let r=u(p,"src",t),e=u(r,"video.config.json");if(!E(e))throw new Error(`Video config not found: ${e}`);let o=JSON.parse($(e,"utf8"));if(o.videoId!==t)throw new Error(`video.config.json declares videoId "${o.videoId}" but directory target is "${t}".`);let n=o.production;if(!n||typeof n!="object")throw new Error("production must be an object.");let i=u(p,"public"),s=u(i,n.publicPath??t),c=f(i,s);if(c===".."||c.startsWith(`..${l}`))throw new Error("production.publicPath must stay inside public/.");return{videoId:t,sourceDir:r,publicDir:s,production:n,resolveConfiguredPath:j}},w=t=>{let r=t.filter(o=>!o.startsWith("--")),e=t.filter(o=>o.startsWith("--")&&o!=="--force");if(e.length>0)throw new Error(`Unknown option: ${e.join(", ")}`);if(r.length!==1)throw new Error("Exactly one video id is required.");return{videoId:r[0],force:t.includes("--force")}};var b=t=>x("sha256").update(C(t)).digest("hex"),y=t=>{let r=h(t),e=r.production.assetLinks??[];if(!Array.isArray(e))throw new Error("production.assetLinks must be an array.");for(let[o,n]of e.entries()){if(!n||typeof n!="object")throw new Error(`production.assetLinks[${o}] must be an object.`);if(typeof n.output!="string"||n.output.length===0)throw new Error(`production.assetLinks[${o}].output must be a non-empty string.`);let i=r.resolveConfiguredPath(n.source,`production.assetLinks[${o}].source`),s=D(r.publicDir,n.output),c=R(r.publicDir,s);if(c===".."||c.startsWith(`..${J}`))throw new Error(`production.assetLinks[${o}].output must stay inside the video's public directory.`);if(!m(i))throw new Error(`Canonical source asset not found: ${i}`);if(S(L(s),{recursive:!0}),!m(s)){g(i,s);continue}let a=A(s);if(a.isSymbolicLink()){P(s),g(i,s);continue}let d=O(i);if(!(a.dev===d.dev&&a.ino===d.ino||a.size===d.size&&b(s)===b(i)))throw new Error(`${s} exists but does not match the configured canonical source.`)}console.log(`Asset links are ready for ${t}.`)};var[I,...W]=process.argv.slice(2);if(I!=="link")throw new Error("Usage: assets.mjs link <video-id>");var{videoId:z}=w(W);y(z);
